# Nested Function
print(round(abs(float(input("Angka yang dimasukkan akan selalu positif: ")))))
# Angka yang dimasukkan akan selalu positif, karena perintah input sudah diberi
# perintah untuk float, absolute, round